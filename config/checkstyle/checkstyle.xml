<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Basic Checkstyle configuration for the upload-client module.
  This is what you will link to as "static analysis configuration" in your assignment.
-->
<module name="Checker">

    <!-- Global settings -->
    <property name="severity" value="warning"/>

    <!-- LineLength must be directly under Checker in recent Checkstyle versions -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <!-- TreeWalker hosts most code-structure rules -->
    <module name="TreeWalker">

        <!-- Indentation rules -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
        </module>

        <!-- Require braces around if/else/for/while blocks -->
        <module name="NeedBraces"/>

        <!-- Basic whitespace rules -->
        <module name="WhitespaceAround"/>
        <module name="WhitespaceAfter"/>

    </module>
</module>
